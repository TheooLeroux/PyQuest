---
titre: L'inventaire du refuge
difficulte: 2
code_depart: |
  # Lis les deux nombres, l'un après l'autre
tests:
  - entrees: ["3", "4"]
    attendu: "Il y a 7 pierres."
  - entrees: ["10", "0"]
    attendu: "Il y a 10 pierres."
  - entrees: ["25", "17"]
    attendu: "Il y a 42 pierres."
indices:
  - « Deux questions, deux réponses. On ne compte bien qu'après avoir tout demandé, héhé. »
  - Appelle `int(input())` deux fois, dans deux variables différentes, puis additionne-les dans une f-string.
  - |
    ```python
    rouges = int(input())
    bleues = int(input())
    # Le calcul peut vivre directement dans les accolades.
    print(f"Il y a {rouges + bleues} pierres.")
    ```
---
Le refuge du vieux site fait l'inventaire : des pierres rouges, des pierres
bleues.

Ton programme lit **deux nombres**, l'un après l'autre (un `input()` chacun),
puis affiche le total sous cette forme exacte :

```
Il y a 7 pierres.
```

(pour `3` puis `4` — le point final compte !)
